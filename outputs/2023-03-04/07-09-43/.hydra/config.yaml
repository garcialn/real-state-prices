default:
- process: process
- model: lgbm-reg
- _self_
raw:
  path: data/raw/properties.parquet
processed:
  dir: data/processed
  name: properties_processed.parquet
  path: ${processed.dir}/${processed.name}
final:
  dir: data/final
  name: properties_final.parquet
  path: ${final.dir}/${final.name}
image:
  footage_hist: images/footage_hist.png
  price_hist: images/price_hist.png
